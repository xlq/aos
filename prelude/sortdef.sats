sortdef igz = {a: int | a > 0}
    and pos = igz
    and ilz = {a: int | a < 0}
    and inz = {a: int | a <> 0}
    and nat = {a: int | a >= 0}
    and agz = {a: addr | a > null}
    and agez = {a: addr | a >= null}
